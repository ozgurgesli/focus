name: Build focus.exe

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          gcc main.c -o focus.exe -lpsapi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: focus
          path: focus.exe
